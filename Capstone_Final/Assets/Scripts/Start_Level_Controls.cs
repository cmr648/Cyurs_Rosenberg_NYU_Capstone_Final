using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Start_Level_Controls : MonoBehaviour
{
    [Header ("Keyboard Controls")] // creating public slots for keyboard controls
    public KeyCode Player_1_Start;
    public KeyCode Player_2_Start;
    public KeyCode Player_3_Start;
    public KeyCode Player_4_Start;

    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
